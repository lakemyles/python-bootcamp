# Did the blackjack capstone project

# use underscore when you don't need the variable
# and you just want to loop N times
for _ in range(2):
    user_cards.append(blackjack.deal_card(deck))
    dealer_cards.append(blackjack.deal_card(deck))
